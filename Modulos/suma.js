const suma = (a,b)=> {
    return a + b
}

module.exports = {suma}

// Desacople de funcionalidades, permite desglozar la funcionalidad en distintos modulos para luego importarlos cuando se requiera.
